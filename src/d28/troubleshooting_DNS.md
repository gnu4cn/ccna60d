# DNS 问题的故障排除

路由器配置中的一条默认命令，将是 `ip domain-lookup`。当这条命令已被禁用时，那么 DNS 就将不工作。有时路由器管理员会禁用他，因为当咱们输入了某条命令时，那么在路由器会执行查询期间，咱们就不得不等待数秒。咱们可以以下命令，关闭 DNS 查找：

`Router(config)#no ip domain-lookup`

访问控制列表通常会阻止 DNS，因此这是造成问题的另一可能原因。咱们可以 `debug domain` 命令，调试路由器上的 DNS。


