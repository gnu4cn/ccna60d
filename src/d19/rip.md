# 路由信息协议

RIPv1 是在网络规模较小时创建的，因此跳数（路由器）限制设置为 15。这是为了防止恶意 RIP 数据包无休止地穿越网络。通过前面的 debug ip rip 输出，你可以看到 RIP 广播更新是有类的。因此，它没有 VLSM 的概念，也就是说，如果你有一个 192.168.1.16/26 的网络，它就会将其广告为 192.168.1.0/24。此外，它不提供身份验证，因此很容易成为攻击者向网络注入虚假路由更新的目标。在下图 19.1 中，您可以看到 RIP 的数据包捕获，其中显示了 RIPv1 使用 UDP 传输和端口 520 的事实。
